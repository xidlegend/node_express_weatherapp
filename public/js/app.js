console.log('Client Side javascript loaded wooo!');

/*     let address = 'Haima';
    let country = 'Oman';
    fetch(`http://localhost:3000/weather?address=${address}&country=${country}`)
    .then(res => {
        const data = res.json();
        return data;
    })
    .then(data => {
        if (data.error) {
            console.log('Error: ' + data.error);
            return;
        }
        console.log(`In ${country} ${address} it is: ${data.description}`);
    }); */
